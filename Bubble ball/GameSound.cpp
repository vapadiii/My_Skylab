#include "GameSound.h"
GameSound::GameSound(float width, float height)
{
	/*
	if (!music.openFromFile("mixkit.wav"))
	{
		//handle eror
	}
	music.setVolume(30);
	music.play();
	music.setLoop(true);
	
	if (!buffer.loadFromFile("sound1.wav"))
	{
		//handle eror
	}
	sound.setBuffer(buffer);
	sound.setVolume(40);
	sound.play();
	*/
}

GameSound::~GameSound()
{

}

void GameSound::playMusic()
{

}
